<% @users.each do |a| %>
    <p>
        
        [ユーザID]
        <%= a.uid %>
        [パスワード]
        <%= a.pass %>
        
        <%= link_to '削除', "/users/#{a.id}", method: :delete %>
        
        
        
    </p>
<% end %>
<p><%= link_to 'アカウント作成', "/users/new" %></p>